"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",

  "electron:dev": "concurrently -k \"cross-env NODE_ENV=development vite\" \"wait-on http://localhost:5173 && electron .\"",

  "electron:clean": "rimraf dist release_v16",

  "electron:dist": "npm run electron:clean && vite build && electron-builder --win"
}
